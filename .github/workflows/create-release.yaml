# generated by hotdog-werx/codeguide - manual changes will be overwritten
name: create-release

on:
  pull_request:
    types: [closed]
    branches: [master]

permissions:
  contents: write
  pull-requests: read

jobs:
  github-release:
    uses: hotdog-werx/codeguide/.github/workflows/_create-release.yaml@topic/repolish
    secrets: inherit
    with:
      build-command: 'poe build'
      publish-command: 'poe publish'

## post-release jobs - add your custom jobs here
