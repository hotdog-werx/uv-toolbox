# generated by hotdogwerx-devkit-workspace@0.1.0 - manual changes may be overwritten
name: deploy-docs

on:
  push:
    branches: [master]

concurrency:
  group: 'pages'
  cancel-in-progress: false

jobs:
  deploy-docs:
    uses: hotdog-werx/devkit/.github/workflows/__workspace_deploy-docs.yaml@topic/repolish
    permissions:
      contents: read
      pages: write
      id-token: write

  ## start-additional-deploy-jobs
  ## post-deploy jobs — add your custom jobs here
  ## end-additional-deploy-jobs
