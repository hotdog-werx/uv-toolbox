# generated by hotdogwerx-devkit-releez@0.1.0 - manual changes may be overwritten
name: Lint PR

on:
  pull_request:
    types:
      - opened
      - reopened
      - edited
      - synchronize

# A reusable workflow can never exceed the permissions granted here by the
# calling (this) workflow, regardless of what it declares for itself.
permissions:
  contents: read

jobs:
  lint-pr-title:
    uses: hotdog-werx/devkit/.github/workflows/__releez_lint-pr-title.yaml@topic/repolish
    with:
      use-self-action: false

  ## start-additional-lint-pr-title-jobs
  ## post-lint-pr-title jobs — add your custom jobs here
  ## end-additional-lint-pr-title-jobs
