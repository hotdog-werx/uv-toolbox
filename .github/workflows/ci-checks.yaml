# generated by hotdog-werx/devkit — manual changes will be overwritten
name: ci-checks

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

jobs:
  repo-checks:
    uses: hotdog-werx/devkit/.github/workflows/__workspace_repo-checks.yaml@topic/repolish
    with:
      enable-docs: true

  python-checks:
    uses: hotdog-werx/devkit/.github/workflows/__python_python-checks.yaml@topic/repolish
    with:
      operating-systems: '["ubuntu-latest", "windows-latest"]'
      codecov: true
    secrets: inherit

## start-custom-ci-checks
## Add custom jobs here (e.g. a tests/test-action pair). Content between
## these two marker lines is preserved verbatim across repolish re-applies
## — GitHub Actions `needs:` only works between jobs in the same file, so
## jobs that must depend on repo-checks/python-checks belong here rather
## than in a separate workflow file.
## end-custom-ci-checks
