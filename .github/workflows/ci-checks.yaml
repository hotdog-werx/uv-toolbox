# generated by hotdog-werx/codeguide - manual changes will be overwritten
name: ci-checks

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

jobs:
  run-checks:
    uses: hotdog-werx/codeguide/.github/workflows/_ci-checks.yaml@topic/repolish
    with:
      operating-systems: '["windows-latest", "ubuntu-latest", "macos-latest"]'

## post-check jobs - add your custom jobs here
